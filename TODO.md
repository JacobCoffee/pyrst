

- resizable panels

- syntax and error highlighting
- dark mode
- themes!
- review rendered output: [Pasted text #1 +1039 lines] you see it
  says we have syntax errors. id like to highliught them in the UI,
  allowing users to jump to errors and warnings to fix them
  System Message: WARNING/2 (<string>, line 165)
  System Message: ERROR/3 (<string>, line 189)
  Unknown interpreted text role "py:func".
- image rendering isnt working (section 8)
- undo functionality
- backticks dont generate code block: ``test`` doesnt work
- certain actions on toolbar like create table need to start new line because it malforms it where it places it.
- When i am on a line and do "bullet" or "numbered" list it should see if the line is empty and if not make the current item
  a bulletted/numbered item not just create 3 placeholder items
- Export/copy as HTML file, PDF file, copy to clipboards
- local storage persistance